services:
  postgres:
    image: postgres:17
    container_name: unitybankx-db

    environment:
      POSTGRES_USER: lokeshkr
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: unitybankx

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
